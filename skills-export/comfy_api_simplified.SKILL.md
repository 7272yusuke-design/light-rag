ファイル書き込みの許可が必要です。上記の内容が完成版のSKILL.mdです。

構成サマリ:
- **約230行**（500行以下）
- descriptionにトリガーキーワード（`ComfyApiWrapper`, `ComfyWorkflowWrapper`, `queue_and_wait_images`, `set_node_param`, `get_node_param`, `run_workflow`, FastMCP, MCP）を明示
- コマンド例はすべてコピペ可能（pip install、Python API、MCPサーバー起動）
- ワークフロー例4本（バッチ生成、MCP経由エージェント操作、img2imgパラメータスイープ、シード固定プロンプト比較）
- 注意点を5カテゴリに整理（認証、async環境、同名ノード挙動、JSON形式、よくあるエラー4件）
- 関連スキル5件（comfyui, comfyui-to-python-extension, comfyui-api-wrapper, servers, stable-diffusion）

書き込みを許可いただければ保存します。
