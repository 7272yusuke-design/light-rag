# データスキーマ設計: ナレッジ構造化ルール

## 設計方針

1. 検索精度 > 網羅性 — 曖昧な情報より、正確で再利用可能な単位を優先
2. メタデータは投入時に付与 — 後から付けるのはコストが高い
3. 1ドキュメント = 1関心事 — 巨大なドキュメントは分割して投入
4. 鮮度は明示的に管理 — 投入日とソースの更新日を記録
5. カテゴリは固定、タグはLLM自由判定 — 大分類は人間が管理、小分類はLLMが付けて後から正規化

---

## ナレッジカテゴリ（固定・大分類）

LLMが自動判定する際、以下から1つ選択する。

### ドメインカテゴリ

| カテゴリ | 説明 | 例 |
|---|---|---|
| framework | フレームワーク・ライブラリの設計思想と使い方 | CrewAI, LangChain, FastAPI, Next.js |
| pattern | 実装パターン・アーキテクチャパターン | マルチエージェント, Pub/Sub, RAG, MVC |
| tool | ツール・CLI・開発支援の使い方 | Docker, git, Claude Code SKILL, Repomix |
| protocol | プロトコル・API仕様 | Virtual Protocol, OpenRouter API, MCP |
| infra | インフラ・デプロイ・運用 | VPS設定, PostgreSQL, Nginx, CI/CD |
| crypto | 仮想通貨・DeFi固有の知識 | DEX, トークン発行, ウォレット連携 |

### 開発領域カテゴリ

| カテゴリ | 説明 | 例 |
|---|---|---|
| website | Webサイト構築（静的・動的・デザイン） | LP, アニメーション, レスポンシブ |
| webapp | Webアプリケーション開発 | 認証, DB設計, API設計, ダッシュボード |
| agent | AI エージェント・自律システム | マルチエージェント, ツール連携, 推論ループ |
| workflow | ワークフロー自動化 | n8n, Make, 自動化パイプライン |

注: 1つのリポジトリが複数カテゴリに該当する場合、最も主要な1つをcategoryに、残りをsub_categoriesに入れる。

---

## タグ運用ルール

### LLM自動タグ付けの指針

LLMは要約時に3〜8個のタグを自由に付与する。ただし以下のルールに従う：

必須タグ:
- 言語タグ（python, typescript, rust など）

推奨タグ:
- ドメインタグ（trading, agent, web3, animation など）
- パターンタグ（multi-agent, rag, pipeline, scroll-effect など）
- 技術タグ（react, nextjs, fastapi, framer-motion など）

禁止:
- 汎用的すぎるタグ（code, development, programming）
- 3単語以上のタグ（multi-agent-orchestration-pattern → multi-agent）
- カテゴリ名と同一のタグ（frameworkをタグとして使わない）

### タグ正規化ルール（normalize_tags.py で自動適用）

全タグは小文字に統一。以下のエイリアスは正規形にマージ：

| 正規化前 | 正規化後 |
|---|---|
| nextjs, next.js, Next | nextjs |
| react, React.js, reactjs | react |
| multi-agent, multiagent | multi-agent |
| TypeScript, Typescript | typescript |
| MCP, model-context-protocol | mcp |
| RAG | rag |
| LLM | llm |
| FastAPI | fastapi |
| Docker | docker |
| SQLite | sqlite |

---

## LightRAG投入テキストのテンプレート

### GitHub リポジトリ用

  # [リポジトリ名]
  ## 基本情報
  - リポジトリ: [GitHub URL]
  - 言語: [主要言語]
  - カテゴリ: [自動判定結果]
  - サブカテゴリ: [自動判定結果]
  - タグ: [自動生成タグ]
  - 最終確認日: [YYYY-MM-DD]
  ## 概要
  [1-3文でリポジトリの目的を説明]
  ## 設計思想
  [アーキテクチャや設計哲学]
  ## 主要コンポーネント
  - [コンポーネント名]: [役割と責務]
  ## 実装パターン
  - [パターン名]: [どういう場面でどう使っているか]
  ## 適用シーン
  [どういうプロジェクトや課題にこの知識が役立つか]
  ## 注意点・制約
  [既知の制限、非推奨事項、ハマりポイント]

### Claude Code SKILL用

  # SKILL: [スキル名]
  ## 基本情報
  - ソース: [公式 / ユーザー作成 / コミュニティ]
  - カテゴリ: tool
  - 対象技術: [docx / pdf / pptx / etc]
  - 最終確認日: [YYYY-MM-DD]
  ## 概要
  [このSKILLが何をするか]
  ## 使用トリガー
  [どういう指示でこのSKILLが発動するか]
  ## 主要手順
  [SKILLの核心的な手順を要約]
  ## ベストプラクティス
  [このSKILLを使う上でのコツ、注意点]
  ## 関連技術
  [このSKILLと組み合わせて使える他の技術やツール]

---

## Obsidian MD出力テンプレート

  ---
  source: [URL or filepath]
  category: [自動判定カテゴリ]
  sub_categories: [サブカテゴリリスト]
  tags: [tag1, tag2, tag3]
  language: [python, typescript, etc]
  ingested: [YYYY-MM-DD]
  source_updated: [YYYY-MM-DD or unknown]
  status: [active / outdated / review-needed]
  ---
  # [タイトル]
  [本文]
  ## 関連ナレッジ
  - [[関連ドキュメント1]]

---

## 鮮度管理ルール

| status | 意味 | アクション |
|---|---|---|
| active | 最新で正確 | そのまま利用可 |
| review-needed | 180日以上更新なし | 再取得・再要約を検討 |
| outdated | 明確に古い情報 | 検索結果に含めるが警告を表示 |

自動チェック: check_freshness.py で投入日から180日経過を検出。--fixで自動的に review-needed へ遷移。

---

## ドキュメント分割ルール

LightRAGのエンティティ抽出精度を保つため：

- 1ドキュメント 3000文字目安以下（日本語の場合）
- 巨大リポジトリは「概要」「主要コンポーネント詳細」「実装パターン集」に分割
- 分割時はドキュメント名にサフィックスを付与（例: crewai-overview, crewai-patterns）

---

## 注意事項

- agency-agentsのようなYAML形式のエージェント定義ファイルはLightRAGのテンプレートに沿わないため投入しない
- 巨大リポジトリ（10M tokens超）はrepomixで0.2%程度しか使えないため、graphifyで事前に構造把握してから重要部分を特定する
- LightRAGは重複排除の自動検知機能あり（同一内容の再投入はスキップされる）

---

## バグ修正パターンテンプレート

投入時は以下の形式に従う。

    ## バグ: <タイトル>（<修正日> 修正）
    - 症状: <ユーザーから見た現象>
    - 原因: <技術的な根本原因>
    - 修正: commit <hash> / <ファイル名・関数名>
    - 再発防止: <テスト方法・チェック観点>
    - 関連モジュール: <ファイル / クラス / メソッド>

---

## ナレッジ3レイヤー定義

| レイヤー | 意味 | 内容 | ファイル名規約 |
|---------|------|------|--------------|
| L3 | 「作り方を知る」 | リポジトリ/ツール/スキルの実装知識 | `*-l3_lightrag.txt` |
| L2 | 「組み合わせ方を知る」 | パターン/手法論（複数L3の接続） | `*-l2_lightrag.txt` |
| L1 | 「状況を知る」 | プロジェクト固有/ドメイン知識 | `*-l1_lightrag.txt` |

### 投入ルール
- **L3**: 最初からL3品質で投入する。L1で入れてからL3に上げる方式は禁止（重複の原因）
- **L2**: 実装・動作確認済みのパターンのみ投入。未検証のアイデアは投入しない
- **L1**: プロジェクト固有の文脈情報。鮮度が最重要（6ヶ月で要レビュー）
- **削除**: DELETE API禁止（全消しバグ）。PostgreSQL直接操作（psql）で行う。操作前にpg_dump必須

### L3必須要素
- 概要（1-2文）
- インストール・セットアップ
- API / 主要インターフェース（引数・戻り値・エラーハンドリング）
- コード例 3件以上（基本・応用・エッジケース）
- ベストプラクティス
- アンチパターン
- 既存ナレッジとの接続

### L2必須要素
- 解決する課題
- 適用条件（いつ使う / いつ使わない）
- 使用するL3ナレッジ一覧
- アーキテクチャ / データフロー
- 接続コード（動作確認済み）
- 判明した制約・工夫
- 再利用条件

### L1必須要素
- プロジェクト概要・目的
- 制約条件・前提
- 関連するL2/L3ナレッジへの参照
- 最終確認日（鮮度管理）

---

## L3: 実装ナレッジテンプレート

    # [ツール/リポジトリ名]
    ## 基本情報
    - リポジトリ: [GitHub URL]
    - カテゴリ: [framework / tool / skill / agent / workflow / infra / website]
    - タグ: [3-8個、小文字]
    - レイヤー: L3（実装ナレッジ）
    - 最終確認日: YYYY-MM-DD
    ## 概要
    [1-2文で何をするツールか]
    ## インストール・セットアップ
    [セットアップ手順]
    ## API / 主要インターフェース
    [引数・戻り値・エラーハンドリング]
    ## コード例
    ### 例1: [基本的な使い方]
    ### 例2: [よくあるユースケース]
    ### 例3: [応用パターン]
    ## ベストプラクティス
    [推奨される使い方]
    ## アンチパターン
    [やってはいけないこと]
    ## 注意点・制約
    [既知の制限、ハマりポイント]
    ## 既存ナレッジとの接続
    - [関連L3ナレッジ]: [接続方法]
    - [関連L2パターン]: [適用場面]

---

## L2: パターンナレッジテンプレート

    # [パターン名]
    ## 基本情報
    - カテゴリ: [pattern / combination]
    - 使用ナレッジ: [L3名をカンマ区切り]
    - タグ: [技術タグ]
    - レイヤー: L2（パターンナレッジ）
    - 最終確認日: YYYY-MM-DD
    ## 解決する課題
    [何のためにこのパターンが必要か]
    ## 適用条件
    - いつ使う: [条件]
    - いつ使わない: [条件]
    ## アーキテクチャ
    [データフロー・接続方法・各パーツの役割]
    ## 接続コード
    [パーツ間をつなぐ核心のコード（動作確認済み）]
    ## 判明した制約・工夫
    [実装中に分かったこと。L3単体では分からない知見]
    ## 再利用条件
    [このパターンを別プロジェクトで使うための前提条件]
    ## 既存ナレッジとの接続
    - [参照するL3ナレッジ]: [どう使うか]

---

## L1: コンテキストナレッジテンプレート

    # [プロジェクト/ドメイン名]
    ## 基本情報
    - カテゴリ: [context / persona / research]
    - タグ: [関連タグ]
    - レイヤー: L1（コンテキストナレッジ）
    - 最終確認日: YYYY-MM-DD
    ## 概要
    [プロジェクトの目的・背景]
    ## 制約条件・前提
    [技術的・ビジネス的な制約]
    ## 関連ナレッジ
    - [参照するL3/L2ナレッジ]: [関係性]

---

## L2候補（L2c）レイヤー ※2026-04-18 追加

### 位置付け

L2（検証済みパターン）とは別の、**ブレスト・OSS読解ベースの未検証パターン**を蓄積するレイヤー。設計出力時の「着想の材料」として使う。

### L2とL2cの違い

| 区分 | L2（検証済み） | L2c（候補） |
|---|---|---|
| 投入条件 | 自分で実装・動作確認済み | ブレスト/OSS読解で「使えそう」と判断 |
| ファイル名 | `*-l2_lightrag.txt` | `*-l2c_lightrag.txt` |
| status | `active` | `unverified` |
| 用途 | 設計の「骨格」（動く前提で組み込める） | 設計の「着想」（方向性のヒント） |
| combination-architectでの扱い | そのまま提案に組み込み | 採用時は「未検証」と明示、検証TODO追加 |

### L2c投入条件（以下のどれか）

1. OSS読解で見つけた設計パターン
2. ブレストで「これは使えそう」と言語化できたアイデア
3. 複数のL3を組み合わせて思いついた新しい設計
4. 案件相談中に生まれた仮の設計案

### L2c必須要素（最小セット）

ブレストの勢いを殺さない最小限の型:

- パターン名（1行で言える）
- 解決する課題（なぜこのパターンが必要か）
- 参照するL3（最低1つ）
- 出典（OSS名 / 自分のブレスト / 案件xx）
- 未検証マーカー（status: unverified）
- 昇格条件（ビジネスイベント形式で具体的に）

### L2c昇格フロー（L2c → L2）

1. L2cパターンを自環境で実装・動作確認
2. L2cドキュメントに「検証結果」「判明した制約・工夫」を追記
3. ファイル名を `-l2c_` → `-l2_` にリネーム
4. KNOWLEDGE-DECISIONS.mdに昇格ログを記載
5. status: unverified → active に変更

### L2c鮮度管理

- 半年（180日）以上検証されないL2cは status: review-needed に遷移
- 1年以上放置されたL2cは削除候補（psql経由で削除）

---

## L2c: 候補パターンテンプレート

    # [パターン名]
    ## 基本情報
    - カテゴリ: pattern-candidate
    - 使用ナレッジ: [参照するL3名をカンマ区切り]
    - タグ: [技術タグ]
    - レイヤー: L2c（候補パターン）
    - status: unverified
    - 出典: [OSS名 / ブレスト / 案件名]
    - 最終確認日: YYYY-MM-DD
    ## 解決する課題
    [何のためにこのパターンが必要か]
    ## 想定アーキテクチャ
    [データフロー・接続方法・各パーツの役割]
    ## 参考実装
    [OSSのコードパス or ブレストメモ]
    ## 昇格条件
    [いつ実装して検証するか。ビジネスイベント形式]
    例: 「OpenClaw本格化時」「n8n案件でマルチエージェント実装を受注したとき」
    ## 関連ナレッジ
    - [参照するL3ナレッジ]: [どう使うか]

---

## ナレッジ投入の判断フロー ※2026-04-18 追加

### 投入経路

| 対象 | 経路 |
|---|---|
| ナレッジ本体（L3/L2/L2c/L1） | Claude.ai LightRAGプロジェクトから upload_document |
| 管理ドキュメント（INDEX/DECISIONS/RESUME/SCHEMA等） | VPS上 /docker/lightrag/docs/ で直接編集 |

### ドメイン境界

- 汎用スキル・パターンのみLightRAGに投入
- 顧客固有情報（認証情報・仕様書・進捗）はClaude Code側で完結
- 自動収集は行わない（手動投入のみ）

### 4つの判断カテゴリ

新しいリソース（リポジトリ/ツール/パターン）評価時の判断:

| カテゴリ | 意味 | 再検討条件の書き方 |
|---|---|---|
| L3投入 | 実装ナレッジとして蓄積 | — |
| L2c投入 | 候補パターンとして蓄積、検証後にL2昇格 | 昇格トリガーをビジネスイベント形式で |
| 保留 | 領域は合うが時期尚早 | フェーズ進展トリガー（例: OpenClaw本格化時） |
| 見送り | 現在のビジネス領域から外れる | 領域進出トリガー（例: 電子署名案件受注時） |

### 再検討条件の書き方ルール

- ❌ 「案件時」「実装時」（漠然・禁止）
- ✅ 「OpenClaw本格化時」「電子署名案件受注時」「n8n→アプリ開発アップセル成立時」（具体ビジネスイベント）

### 判断ログの記録先

- 投入・L2c・保留・見送りの全判断を KNOWLEDGE-DECISIONS.md に記録
- 再評価時は必ずDECISIONSを参照して重複判断を避ける
- DECISIONSの項目: 対象名 / 判断 / 理由 / 日付 / 再検討条件 / 出典URL

### 投入前の必須チェック

1. **重複チェック**: search_knowledge で既存ナレッジと被らないか確認
2. **境界チェック**: 汎用か？ 顧客固有情報を含まないか？
3. **判断確定**: 4つのカテゴリのどれか決定
4. **DECISIONS記録**: 判断結果をKNOWLEDGE-DECISIONS.mdに追記

---

## L1再定義 ※2026-04-18 v3追加

### 背景

初期のL1定義「プロジェクト固有/ドメイン知識」は、実運用で以下の問題を起こしていた:

1. プロジェクト進捗・仕様書・ペルソナがLightRAGに蓄積され、検索ノイズ化
2. Claude.aiプロジェクト側・Notion等と情報が重複
3. 低優先プロジェクトのL1が放置され、鮮度管理が機能せず
4. 発信用途で顧客固有情報が混入するリスク

活用目的を逆算すると、L1に残すべきは「L2/L3を正しく使うための前提」のみと判明。

### L1の新しい役割

L2/L3を参照する際の**横断運用のメタ知識**および**環境前提**。
プロジェクト固有情報はLightRAGに投入しない（Claude.aiプロジェクト/プロジェクト管理ツールで管理）。

### L1のサブタイプ

| サブタイプ | 内容 | 例 |
|---|---|---|
| L1-Infra | 開発環境・インフラの横断的前提 | VPS構成、DB接続設定、CLIツール運用、Hostinger特性 |
| L1-Ops | LightRAG運用自体のメタ知識 | ナレッジ投入ルール運用知見、psql削除手順、pg_dump運用 |

### L1投入条件

以下の全てを満たすものだけL1投入:

1. 複数プロジェクトで再利用される知識である
2. 顧客固有情報・進捗情報・ペルソナを含まない
3. L2/L3の参照時に文脈として必要になる

### L1から外すもの（v3で除外決定）

- 特定プロジェクトの進捗・仕様書 → Claude.aiプロジェクトまたはNotion
- ペルソナ・個人設定 → Claude.aiプロジェクトメモリ
- 案件固有の実装詳細 → 汎用化してからL2cへ
- 低優先プロジェクトのコンテキスト → LightRAG外（または削除）
- 旧・残骸 → 削除

### L1→L2c昇格フロー

案件完了時、L1から以下を抽出してL2cへ昇格可能:

1. 汎用化できる手法論
2. 他プロジェクトで再利用できる設計判断
3. 複数プロジェクトで観察されたアーキテクチャパターン

昇格手順:
- L1の該当部分を抽出し、L2cテンプレートで新規ドキュメント化
- ファイル名: `*-l2c_lightrag.txt`
- 元のL1ドキュメントから該当記述を削除
- KNOWLEDGE-DECISIONS.mdに昇格ログを記載

### L1鮮度管理

- L1-Infra / L1-Ops とも6ヶ月で要レビュー（status: review-needed）
- プロジェクト情報を含むL1は**v3移行で全件見直し対象**
- レビュー時は「現在も複数プロジェクトで再利用されているか」を基準に判断

### ファイル名規約

| サブタイプ | ファイル名 | 例 |
|---|---|---|
| L1-Infra | `*-l1-infra_lightrag.txt` | `dev-environment-workflow-l1-infra_lightrag.txt` |
| L1-Ops | `*-l1-ops_lightrag.txt` | `lightrag-knowledge-ops-l1-ops_lightrag.txt` |

既存 `*-l1_lightrag.txt` は移行期間中併存可。v3整理タスクで順次リネーム。

### L2の品質ゲート強化（v3で追加）

L2へのプロジェクト固有情報の混入を防ぐため、以下をL2の必須要件に追加:

1. 汎用性の検証: 最低2つ以上のドメイン・プロジェクトで適用可能であることを記載
2. カテゴリ制限: L2の カテゴリ は pattern / combination のみ。context / persona / research は L2 禁止
3. L1→L2直接昇格の禁止: L1 → L2c → 検証 → L2 の段階を経ること

v3移行時にL2混入を発見した場合は、該当ドキュメントをL1もしくはL2cへリネームするか削除する。

---

## 検索モード運用ガイド（2026-04-19 Phase 3検証結果を反映）

### LightRAG の4検索モード

LightRAGの`search_knowledge`は4つの検索モード（hybrid / local / global / naive）を提供する。Phase 3 の実地検証で判明した各モードの挙動と、用途別の使い分けを以下にまとめる。

| モード | 動作 | 強み | 弱み |
|---|---|---|---|
| **hybrid**（デフォルト） | ベクトル類似 + エンティティグラフの両方を使って結果を合成 | 複数関連ナレッジが揃う、設計判断で比較候補が漏れない | 単一固有名詞では競合ナレッジに埋もれる場合あり |
| **local** | 特定エンティティに関連するチャンクを深掘り | 固有の技術要素・概念の詳細が出る | 横断的な傾向は見えない |
| **global** | 全体的な関係性・テーマで検索 | 抽象的な問い・全体像に強い | 具体実装は弱い |
| **naive** | 純粋ベクトル検索のみ（エンティティグラフ不使用） | 高速、デバッグ用、埋め込みの品質測定 | 実用精度は hybrid に劣る |

### 用途別の推奨モード

| 用途 | 推奨モード | クエリ例 |
|---|---|---|
| **① 設計書出力**（複数候補を比較したい） | hybrid | 「マルチエージェント取引判断」→ TrinityCouncil + LangGraph + CrewAI + openclaude が揃う |
| **② 開発支援**（特定技術の深掘り） | local | 「hierarchical manager crew」→ CrewAIのProcess.Hierarchical詳細 |
| **② 開発支援**（関連を含めて知りたい） | hybrid | 「n8n ワークフロー AI エージェント」→ n8n + langgraph + paseo |
| **③ 情報発信**（題材素材集め） | hybrid + 2段階クエリ | 詳細は下記「2段階クエリ戦略」参照 |
| **デバッグ・エンティティ純度の点検** | naive | エンティティグラフ不使用で、ベクトル類似のみ確認 |
| **全体トレンド・俯瞰** | global | 「最近の技術傾向」「エージェント全般」 |

### クエリ設計の実践知（Phase 3の学び）

#### 1. 短い固有名詞（4-8文字）は誤ヒットしやすい

ベクトル空間では文字列類似度の影響が残るため、短い固有名詞単独だと別プロジェクトとの取り違えが起こる。
- 悪い例: `crewai` → `crawl4ai` に誤ヒット
- 良い例: `crewai エージェント オーケストレーション` → crewai-l3筆頭

#### 2. 文脈語を必ず1つ以上加える

技術名 + 用途語 + 文脈語の3語構成が最も安定する:
- `n8n` → 不安定
- `n8n ワークフロー AI エージェント` → 安定

#### 3. 動詞（「記事にしたい」「作りたい」）に注意

クエリ内の動詞は題材と無関係でも強くヒットに影響する。
- 悪い例: 「トークン削減パターンを記事にしたい」→ note系・instagrapi・tweepyが優先（動詞に引っ張られる）
- 良い例（2段階）: 
  1. 題材検索: 「トークン削減 rtk mcp2cli プロキシ」
  2. 発信パターン検索: 「note 記事 パターン」

#### 4. 用途③（情報発信）には2段階クエリ

用途③では単一クエリで完結させず、題材検索 → 発信パターン検索 → Claude.ai側で合成、の3段階に分ける。発信用スキル化は残タスク。

### エンティティ純度の運用ルール

Phase 3の検証で、ナレッジ投入方法が検索精度を大きく左右することが判明:

- ❌ **悪い投入**: リポジトリ全体を Repomix で丸ごと投入 → 周辺エンティティ（Tokyo / Gmail / Slack 等）が汚染
- ✅ **良い投入**: L3テンプレートに沿って固有名詞に絞って整理 → 純度の高いエンティティ抽出

新規L3投入時は以下を必須化:
1. 投入前に「そのリポジトリ/ツール固有の名前（クラス・API・概念）」を明示
2. サンプルコード内の地名・個人名・無関係ツール名は除外
3. 「既存ナレッジとの接続」セクションで、関連する他L3/L2を明示的に列挙

### 検索モード未使用時のフォールバック

`search_knowledge` で期待通りの結果が得られない場合の順:

1. **hybrid でヒットなし** → local に切替、特定エンティティで再検索
2. **localでもヒット不十分** → クエリに文脈語を追加して hybrid
3. **それでも不足** → naive で純粋ベクトル類似を確認（エンティティ抽出の問題か、埋め込みの問題かを切り分け）
4. **全体像が見たい** → global で俯瞰

---

## L2c→L2 昇格ルール（verified定義D / 2026-06-13確定）

### verified タグ（検証の出所3段階）

| タグ | 意味 | L2昇格 |
|---|---|---|
| `verified:self` | Yusuke自身が実プロジェクトで適用し機能を確認（このLightRAG基盤上の実運用・PoC含む） | **可** |
| `verified:oss` | OSS/論文/事例で実証されたパターンを確認、自社未適用 | 不可（L2c維持） |
| `verified:thirdparty` | 信頼できる第三者の事例報告で確認、自社・OSSとも未実証 | 不可（L2c維持） |

### 昇格方式: pull型（実案件駆動）

- L2昇格は「在庫レビューで一括昇格」（push型）ではなく、**実案件でそのL2cパターンを実際に使い機能を確認した時点**で個別昇格（pull型）
- 昇格手順:
  1. pg_dump バックアップ（必須）
  2. `doc_name` を `-l2c_lightrag.txt` → `-l2_lightrag.txt` に UPDATE
  3. 本文 `status:unverified` → `status:verified` + `verified:self` タグ追記
  4. record_decision で記録
- `id`（doc-ハッシュ）は変更しない（chunks/entities 参照の整合保持）

### 自己設計だが未実証のケース

- 「Yusuke自身の設計から抽出したが未構築」のものは `verified:self` を**付けない**。実装・実運用された時点で初めて self 該当
- このタイプは昇格先が L2 ではなく **L3 になる場合がある**（ドメイン技術＝「作り方を知る」性質の時）。その場合の昇格条件は「複数プロジェクトでの再利用実証」
- 例: dealcopilot-domain-patterns-l2c（DealCopilotは企画〜テスト構築段階・未構築。設計解像度は高いが verified:self 非該当、昇格先は L2 ではなく L3 想定）

### 2026-06-13 棚卸し結果（参考）

- 総数 207件（L3:157 / L2c:26 / L2:19 / L1:3 / other:2）
- L2c 26件レビュー → L2昇格 0件（いずれも「検証済みの組み合わせパターン」要件を未充足）
- L2比率 9.2%（目標20-25%に未達）。改善は pull型昇格の正規ルートに依存
